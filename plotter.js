function togglePlotter(){S.showPlotter=!S.showPlotter,!S.showPlotter||S.pixelsPerNM?(updateUI(),draw()):startCalibration()}function toggleLock(){S.locked=!S.locked,updateUI(),updateHeading()}function getHeading(){let a=90-180*S.cRot/Math.PI;return a=(a%360+360)%360,Math.round(a)||360}function getReciprocal(){return(getHeading()+180-1)%360+1}function updateHeading(){const a=document.getElementById("panel-hint");if(S.showPlotter&&S.pixelsPerNM&&!S.calibrating){const e=getHeading(),n=getReciprocal();a.innerHTML=`<span class="panel-section"><span class="heading-label">TC</span><span class="heading-big">${String(e).padStart(3,"0")}&deg;</span></span><span class="vsep"></span><span class="panel-section"><span class="heading-label">Recip</span><span class="heading-small">${String(n).padStart(3,"0")}&deg;</span></span><span class="vsep"></span><span class="${S.locked?"status-locked":"status-unlocked"}">${S.locked?"&#x1F512; Locked":"&#x1F513; Unlocked"}</span>`}}