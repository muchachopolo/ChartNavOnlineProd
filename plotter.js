function togglePlotter(){S.showPlotter=!S.showPlotter,!S.showPlotter||S.pixelsPerNM?(S.showPlotter&&centerPlotter(),updateUI(),draw()):startCalibration()}function centerPlotter(){const a=viewport.clientWidth,s=viewport.clientHeight,n=s2i(a/2,s/2);S.px=n.x,S.py=n.y}function toggleLock(){S.locked=!S.locked,updateUI(),updateHeading()}function getHeading(){let a=90-180*S.cRot/Math.PI;return a=(a%360+360)%360,Math.round(a)||360}function getReciprocal(){return(getHeading()+180-1)%360+1}function updateHeading(){const a=document.getElementById("panel-hint");if(S.showPlotter&&S.pixelsPerNM&&!S.calibrating){const s=getHeading(),n=getReciprocal(),e=tcToMH(s);let t=`<span class="panel-section"><span class="heading-label">TC</span><span class="heading-big">${String(s).padStart(3,"0")}&deg;</span></span>`;if((0!==S.magVar||S.windSpd>0)&&(t+='<span class="vsep"></span>',t+=`<span class="panel-section"><span class="heading-label">MH</span><span class="heading-big">${String(e).padStart(3,"0")}&deg;</span></span>`),S.windSpd>0&&S.tas>0){const a=calcWind(s,0);t+='<span class="vsep"></span>',t+=`<span class="panel-section"><span class="heading-label">WCA</span><span class="heading-small">${(a.wca>=0?"+":"")+a.wca}&deg;</span></span>`,t+='<span class="vsep"></span>',t+=`<span class="panel-section"><span class="heading-label">WMH</span><span class="heading-small">${String(a.mh).padStart(3,"0")}&deg;</span></span>`,t+='<span class="vsep"></span>',t+=`<span class="panel-section"><span class="heading-label">GS</span><span class="heading-small">${a.gs}kt</span></span>`}t+='<span class="vsep"></span>',t+=`<span class="panel-section"><span class="heading-label">Recip</span><span class="heading-small">${String(n).padStart(3,"0")}&deg;</span></span>`,t+='<span class="vsep"></span>',t+=`<span class="${S.locked?"status-locked":"status-unlocked"}">${S.locked?"&#x1F512; Locked":"&#x1F513; Unlocked"}</span>`,a.innerHTML=t}}